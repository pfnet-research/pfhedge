# PFHedge 开发环境管理指南

## 🎯 当前环境问题分析

### 发现的问题：
1. **环境混乱**：在base环境中安装了pfhedge，而不是在专门的环境中
2. **pip脚本缺失**：`pip-script.py`文件缺失，导致pip命令异常
3. **依赖不完整**：pfhedge环境中缺少核心依赖（如torch）
4. **版本管理不清**：多个环境版本不一致

### 解决方案：
重新创建一个干净、完整的开发环境

## 🚀 快速设置（推荐）

### 方法1：使用自动脚本
```bash
# 在项目根目录运行
setup_environment.bat
```

### 方法2：手动设置
```bash
# 1. 删除旧环境
conda env remove -n pfhedge -y

# 2. 创建新环境
conda create -n pfhedge python=3.12 -y

# 3. 激活环境
conda activate pfhedge

# 4. 安装PyTorch
pip install torch torchvision torchaudio

# 5. 安装项目依赖
pip install -e ".[dev]"
```

## 📋 环境验证

### 验证步骤：
```bash
# 1. 检查环境
conda env list
conda activate pfhedge

# 2. 验证Python版本
python --version  # 应该是 3.12.x

# 3. 验证核心包
python -c "import torch; print('PyTorch:', torch.__version__)"
python -c "import pfhedge; print('PFHedge:', pfhedge.__version__)"

# 4. 运行测试
pytest -v

# 5. 运行示例
python examples/example_readme.py
```

## 🛠️ 日常开发工作流

### 每日开始工作：
```bash
# 1. 激活环境
conda activate pfhedge

# 2. 进入项目目录
cd C:\Users\jimmy\Documents\workspace1\pfhedge-ty

# 3. 检查环境状态
python -c "import torch, pfhedge; print('环境正常')"
```

### 代码质量检查：
```bash
# 格式化代码
black .

# 整理导入
isort .

# 代码检查
flake8 pfhedge

# 类型检查
mypy pfhedge

# 运行测试
pytest
```

### 提交前检查：
```bash
# 完整检查清单
pytest --cov=pfhedge
black --check .
isort --check .
flake8 pfhedge
mypy pfhedge
```

## 🔧 环境管理命令

### Conda命令：
```bash
# 环境管理
conda env list                    # 列出所有环境
conda create -n env_name python=3.12  # 创建环境
conda activate env_name           # 激活环境
conda deactivate                  # 退出环境
conda env remove -n env_name      # 删除环境

# 包管理
conda list                        # 列出已安装包
conda install package_name        # 安装包
conda remove package_name         # 删除包
conda update conda                # 更新conda
```

### Pip命令：
```bash
# 包管理
pip list                          # 列出已安装包
pip install package_name          # 安装包
pip install -e .                  # 安装当前项目（开发模式）
pip freeze > requirements.txt     # 导出依赖列表
```

## 🐛 常见问题解决

### 1. 环境激活失败
```bash
# 解决方案
conda init powershell  # 或 conda init cmd
# 重启终端
```

### 2. 包安装失败
```bash
# 清理缓存
conda clean --all
pip cache purge

# 重新安装
pip install --no-cache-dir package_name
```

### 3. 版本冲突
```bash
# 查看冲突
pip check

# 重新创建环境
conda env remove -n pfhedge -y
conda create -n pfhedge python=3.12 -y
```

### 4. CUDA/GPU问题
```bash
# 检查CUDA
python -c "import torch; print('CUDA可用:', torch.cuda.is_available())"

# 重新安装PyTorch（GPU版本）
pip uninstall torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

## 📁 项目结构说明

```
pfhedge-ty/
├── pfhedge/              # 核心代码
├── examples/             # 示例代码
├── tests/               # 测试代码
├── docs/                # 文档
├── setup_environment.bat # 环境设置脚本
├── pyproject.toml       # 项目配置
└── 环境管理指南.md      # 本文件
```

## 🎯 最佳实践

### 1. 环境隔离
- ✅ 每个项目使用独立环境
- ❌ 避免在base环境中安装项目包

### 2. 版本管理
- ✅ 使用固定版本号
- ✅ 定期更新依赖
- ❌ 避免使用最新版本（latest）

### 3. 代码质量
- ✅ 提交前运行完整测试
- ✅ 使用代码格式化工具
- ✅ 遵循项目代码规范

### 4. 备份和恢复
```bash
# 导出环境
conda env export > environment.yml

# 恢复环境
conda env create -f environment.yml
```

## 📞 获取帮助

- **项目文档**: [https://pfnet-research.github.io/pfhedge/](https://pfnet-research.github.io/pfhedge/)
- **GitHub Issues**: [https://github.com/pfnet-research/pfhedge/issues](https://github.com/pfnet-research/pfhedge/issues)
- **Conda文档**: [https://docs.conda.io/](https://docs.conda.io/)
- **PyTorch文档**: [https://pytorch.org/docs/](https://pytorch.org/docs/)

---

**记住**: 始终在正确的环境中工作，保持环境的清洁和一致性！ 